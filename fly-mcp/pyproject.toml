[project]
name = "fly-mcp"
version = "1.0.0"
description = "MCP de la mosca-unified: score, scan campeon, sync de estado, exit plan"
requires-python = ">=3.10"
dependencies = ["mcp", "requests", "numpy", "scikit-learn"]
